/*
 * Core/board.h - OBSOLETO. No editar.
 *
 * El mapa de pines del proyecto vive en un unico lugar:
 *
 *      app/inc/board.h
 *
 * Este archivo quedo de una version vieja y solo se mantiene como puente,
 * por si algun include path resuelve "board.h" contra Core/ antes que contra
 * app/inc/. Se puede borrar del proyecto sin problemas.
 */

#include "../app/inc/board.h"
